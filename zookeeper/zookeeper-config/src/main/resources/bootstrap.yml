server:
  port: 8085
spring:
  application:
    name: zookeeper-config
  profiles:
    active: dev
  cloud:
    zookeeper:
      connect-string: server:2181
      config:
        root: config
        profile-separator: ','
